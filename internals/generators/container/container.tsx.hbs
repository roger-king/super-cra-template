import React from 'react';
import styled from '{{ StyledLibraryImport }}';

const Div = styled.div``;

export interface {{ pascalCase ContainerName }}Props {

}

export const {{ pascalCase ContainerName }}: React.FC<{{ pascalCase ContainerName }}Props> = (props: {{ pascalCase ContainerName }}Props) => {
    return (
        <Div>{{ ContainerName }}</Div>
    )
}